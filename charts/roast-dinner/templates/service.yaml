apiVersion: v1
kind: Service
metadata:
  name: {{ include "roast-dinner.fullname" . }}
  labels:
    {{- include "roast-dinner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "roast-dinner.selectorLabels" . | nindent 4 }}
